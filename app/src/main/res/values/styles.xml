<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- ActionBar -->
        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>
    </style>

    <style name="AppTheme.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>


    <!--style name="ContentBlocksTheme.AudioPlayer">
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="audio_player_button_tint">@color/colorPrimaryDark</item>
    </style>

    <style name="ContentBlocksTheme.AudioPlayer.MovingBars">
        <item name="barColor">@color/colorPrimaryDark</item>
    </style>

    <style name="ContentBlocksTheme.AudioPlayer.ProgressBar" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/app_progress_bar</item>
        <item name="android:minHeight">6dp</item>
    </style>

    <style name="ContentBlocksTheme.Ebook">
        <item name="android:background">@android:color/holo_blue_bright</item>
        <item name="android:tint">@android:color/black</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="ContentBlocksTheme.Download">
        <item name="calendar_background_color">@android:color/holo_blue_bright</item>
        <item name="calendar_tint_color">@android:color/black</item>
        <item name="contact_background_color">@android:color/holo_blue_dark</item>
        <item name="contact_tint_color">@android:color/holo_green_light</item>
        <item name="default_background_color">@color/downloadblock_default_background_color</item>
        <item name="default_tint_color">@color/downloadblock_default_tint_color</item>
    </style-->

</resources>
